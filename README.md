
  # User Dashboard Layout

  This is a code bundle for User Dashboard Layout. The original project is available at https://www.figma.com/design/7cBPDZX29Nj34yyzyEscyj/User-Dashboard-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  